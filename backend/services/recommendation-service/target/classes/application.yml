server:
  port: 8085

spring:
  application:
    name: RECOMMENDATION-SERVICE

  data:
    mongodb:
      uri: mongodb://${MONGODB_HOST:localhost}:${MONGODB_PORT:27021}/recommendation_db

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
