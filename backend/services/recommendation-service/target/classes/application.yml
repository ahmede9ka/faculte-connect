server:
  port: 8085

spring:
  application:
    name: RECOMMENDATION-SERVICE

  data:
    mongodb:
      uri: mongodb://${MONGODB_USERNAME:maher}:${MONGODB_PASSWORD:maher}@${MONGODB_HOST:localhost}:${MONGODB_PORT:27018}/recommendation_db?authSource=admin

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
